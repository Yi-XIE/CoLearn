# CoLearn 启动集成指南

这份指南对应当前 Demo 完整版，目标是让你从零到可运行。

## 1. 先决条件

需要先有一份可用的 NanoBot 配置：

默认路径：

```text
~/.nanobot/config.json
```

至少要保证这些字段可用：

- `provider`
- `model`
- 对应 provider 的 API key 或登录凭据

如果这些没有配置，WebUI 可以起来，但真正的学习回合不会正常完成。

## 2. 安装

在仓库根目录执行：

```bash
pip install -e .
```

如果要跑测试：

```bash
pip install -e ".[dev]"
```

如果要验证前端：

```bash
cd third_party/nanobot-0.2.1/webui
npm ci
```

## 3. 生成 Wiki 索引

如果你修改了 `knowledge/wiki/` 下的内容，需要重新生成索引：

```bash
python -m colearn.colearn_wiki.colearn_indexer
```

输出目录默认是：

```text
knowledge/generated
```

## 4. 推荐启动方式

### WebUI

最推荐的演示入口：

```bash
python run_colearn.py webui --port 8080
```

说明：

- 复用 NanoBot 原生 gateway
- 启动时自动挂上 CoLearn
- Settings > Apps 可看到 CoLearn
- `/learn`、`/colearn` 会进入真实宿主命令链

### HTTP API

```bash
python run_colearn.py serve --port 8765
```

### 单轮运行

```bash
python run_colearn.py run --message "I want to learn machine learning" --session user-42
```

### 交互 REPL

```bash
python run_colearn.py chat --session user-42
```

## 5. 常用参数

| 参数 | 默认值 | 说明 |
|---|---|---|
| `--config` | `~/.nanobot/config.json` | NanoBot 配置文件 |
| `--workspace` | 配置中的 workspace | 覆盖工作目录 |
| `--state-root` | `.colearn/state/sessions` | CoLearn 学习状态目录 |
| `--wiki-index-dir` | `knowledge/generated` | Wiki 索引目录 |
| `webui --port` | NanoBot 默认端口 | WebUI 监听端口 |
| `serve --port` | NanoBot API 默认端口 | HTTP API 监听端口 |

## 6. 运行后怎么验证

推荐按这个顺序验证：

1. 启动 WebUI
2. 打开命令面板，确认 `/learn`、`/colearn` 可见
3. 输入：

```text
/learn linear algebra basics
```

4. 再输入：

```text
/colearn
```

5. 打开 Settings > Apps
6. 确认 CoLearn 卡片可见
7. 展开 CoLearn，确认能看到：
   - 当前 goal
   - active node
   - progress
   - graph 节点/边摘要

## 7. 宿主 API

启动后，CoLearn 只读接口可通过宿主访问：

- `/api/v1/colearn/blackboard/current`
- `/api/v1/colearn/graph/current`
- `/api/v1/colearn/session/current`
- `/api/settings/colearn-apps`

这些接口沿用 NanoBot 当前 WebUI / HTTP 鉴权。

## 8. 常见问题

### 8.1 WebUI 能打开，但学习不响应

优先检查：

- NanoBot `provider` 是否已配置
- `model` 是否存在
- API key 是否有效

### 8.2 CoLearn 没有显示图谱内容

优先检查：

- `knowledge/generated` 是否已生成
- 当前是否已通过 `/learn` 建立学习 session

### 8.3 Windows 下 session 保存失败

当前分支已经修复了 Windows 对 `cli:direct` 这类 session key 的路径兼容问题。

如果还遇到旧数据目录异常，删除对应测试残留目录后重试即可。

### 8.4 只看到空态

这是允许的稳定行为。没有 active session、没有 goal、没有 active node 时，CoLearn API 和 Apps 页都应返回空态，不应报 500。

## 9. 嵌入宿主代码

如果你不是走 `run_colearn.py`，也可以显式安装：

```python
import asyncio

from nanobot import Nanobot
from colearn.colearn_runtime import install_colearn

bot = Nanobot.from_config("~/.nanobot/config.json")
install_colearn(bot)

result = asyncio.run(
    bot.run("I want to learn machine learning", session_key="user-42")
)
print(result.content)
```

如果要自定义 CoLearn plugin 参数：

```python
from colearn.colearn_plugin import CoLearnPlugin
from colearn.colearn_runtime import install_colearn

plugin = CoLearnPlugin({
    "state_root": ".colearn/state/sessions",
    "wiki_index_dir": "knowledge/generated",
})

install_colearn(bot, plugin=plugin)
```

## 10. 回归验证命令

后端重点测试：

```bash
pytest -q tests/test_plugin_smoke.py tests/test_colearn_api.py tests/test_state_store.py tests/test_runtime_integration.py tests/test_plugin_host_smoke.py tests/test_colearn_command.py
```

NanoBot 宿主重点测试：

```bash
set PYTHONPATH=D:\CoLearn-plugins;D:\CoLearn-plugins\third_party\nanobot-0.2.1
python -m pytest -q third_party/nanobot-0.2.1/tests/agent/test_runner_hooks.py third_party/nanobot-0.2.1/tests/command/test_model_command.py third_party/nanobot-0.2.1/tests/channels/test_websocket_channel.py third_party/nanobot-0.2.1/tests/channels/test_websocket_http_routes.py
```

前端验证：

```bash
cd third_party/nanobot-0.2.1/webui
npm test -- --run src/tests/api.test.ts src/tests/settings-view.test.tsx
npm run build
```
