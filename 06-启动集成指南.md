# 启动 NanoBot 时加载 CoLearn 插件

本文件说明如何在启动 NanoBot 时挂载 CoLearn 插件。CoLearn 由两部分接入宿主：

| 能力 | 接入方式 | 是否需要启动代码 |
|---|---|---|
| `/colearn` 看板工具 | `nanobot.tools` entry point 自动发现 | 否（`pip install` 后自动） |
| 学习 hooks（会话绑定、模式路由、上下文注入、黑板监控、收尾） | 启动时显式注入 `AgentLoop` | 是 |

NanoBot v0.2.1 没有 hook 插件入口，hooks 只能通过 `AgentLoop(hooks=...)` 或
`_extra_hooks` 注入。因此学习链路必须由一段启动代码安装，`install_colearn(...)`
就是这段代码。

## 1. 安装

```bash
pip install -e .            # 在 CoLearn-plugins 仓根目录
```

安装后，NanoBot 的 `ToolLoader` 会通过 `entry_points(group="nanobot.tools")`
自动发现 `ColearnDashboardTool`，`/colearn` 工具即可用。

## 2. 启动脚本

仓库自带一个开箱即用的启动器 `run_colearn.py`，三种模式：

```bash
# OpenAI 兼容 HTTP API（最常用，等价于 `nanobot serve` + CoLearn 自动挂上）
python run_colearn.py serve --port 8765

# 一次性 SDK 调用，跑完打印结果
python run_colearn.py run --message "我想学机器学习" --session user-42

# 简易 REPL，固定到一个 CoLearn 会话
python run_colearn.py chat --session user-42
```

通用参数：

| 参数 | 默认 | 说明 |
|---|---|---|
| `--config` | `~/.nanobot/config.json` | NanoBot 配置文件路径 |
| `--workspace` | 配置中的 workspace | 覆盖工作目录 |
| `--state-root` | `.colearn/state/sessions` | CoLearn 会话状态目录 |
| `--wiki-index-dir` | `knowledge/generated` | Wiki 生成索引目录 |

如果你想嵌入到自己更复杂的入口里，模板很短：

```python
import asyncio
from nanobot import Nanobot
from colearn.colearn_runtime import install_colearn

bot = Nanobot.from_config("~/.nanobot/config.json")
install_colearn(bot)          # 挂上 4 个学习 hooks + /colearn 工具
result = asyncio.run(
    bot.run("我想学机器学习", session_key="user-42")
)
print(result.content)
```

`install_colearn` 接受三类宿主对象：

- `Nanobot` 门面（自动解析其 `_loop`）
- `AgentLoop` 实例
- 任何暴露 `_extra_hooks` 和/或 `tools` 的兼容对象

它是幂等的：重复调用不会重复注册工具或 hooks。

## 3. 自定义配置

```python
from colearn.colearn_plugin import CoLearnPlugin
from colearn.colearn_runtime import install_colearn

plugin = CoLearnPlugin({
    "state_root": ".colearn/state/sessions",   # 会话状态目录
    "wiki_index_dir": "knowledge/generated",   # Wiki 索引目录
})
install_colearn(bot, plugin=plugin)
```

## 4. 会话语义

- 每个 turn 由 `SessionBinderHook` 通过 `resolve_session_id(context)` 绑定到一个
  CoLearn 会话；优先取 `session_id` / `session_key` / `user_id`，否则回落 `default`。
- `CoLearnPreflightHook` 判断 `SessionMode`（CHAT / LEARNING）与 `TurnMode`
  （LEARN / CHECK / PAUSED），仅在 LEARNING 时注入 Wiki 学习上下文。
- `BlackboardMonitorHook` 在每个 turn 开始/结束静默更新 `blackboard.runtime`。
- 学习真相写入 `.colearn/state/<session>/session.json`，不写进宿主 session。

## 5. Wiki 索引

修改 `knowledge/wiki/` 下的页面后，重建索引：

```bash
python -m colearn.colearn_wiki.colearn_indexer
```

生成物（`wiki_index.json` / `wiki_link_graph.json` / `wiki_search_index.json`）
写入 `knowledge/generated/`，该目录默认不纳入版本控制。

## 6. 验证

```bash
pip install -e ".[dev]"
python -m pytest tests/ -q
```

`tests/test_plugin_host_smoke.py` 与 `tests/test_runtime_integration.py` 会针对
`third_party/nanobot-0.2.1/` 真实快照验证 hooks 继承、工具注册执行与启动注入。
