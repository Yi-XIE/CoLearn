# LightRAG 作为背景知识库的状态机协同方案

这份文档已并入新版主文档：

- `02-Architecture/CoLearn-LightRAG-学习状态机瘦身评估.md`

保留这个文件只是为了兼容旧引用，避免知识分叉。关于 `LightRAG`、`Learning Mode`、`PlanStage`、`LearningBoard`、`web search` 和新版瘦身方案的统一说明，请只维护主文档，不要再在这里继续扩写。
