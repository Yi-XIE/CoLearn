# CoLearn WebUI 接管状态

这份文档只记录当前已接管的结果。

## 当前状态

- `webui` 已成为 CoLearn 的前端主线。
- 主导航页面已消费真实数据。
- 旧 `web/` 已删除。

## 已完成

- 聊天、知识花园、记忆、技能、设置页面已接入当前主线数据。
- 启动与构建入口已对齐到 `webui + runtime_v2`。

## 当前边界

- 新前端能力只进 `webui`。
- 文档不再保留旧前端扩展计划。
