# CoLearn 插件化实施计划

> 配套文档：[01-技术设计.md](./01-技术设计.md)  
> 总工作量估算：**11-14 个工作日**  
> 策略：先定协议，再落 `Wiki + 状态机 + 黑板 + NanoBot 薄适配层`  
> 首版目标：让 CoLearn 以插件形式稳定挂在 NanoBot 上，同时把知识真相与学习状态留在 CoLearn 自己手里  
> 范围约束：**首版明确不做 LightRAG 层**

命名规范同步采用：

- Brand Name: `CoLearn`
- Package Name: `colearn`
- Plugin Name: `colearn`
- 新增代码文件 / 目录前缀：`colearn_*`
- State Dir: `.colearn/`

## 路线图概览

```text
Phase 0  (1d)   定边界: 宿主接口 + Wiki schema + 黑板 schema
   │
Phase 1  (2d)   知识底座: Wiki 文件结构 + 生成索引 + 查询接口
   │
Phase 2  (3d)   学习内核: 状态机 + 黑板 + 写回链路插件化
   │
Phase 3  (2-3d) 宿主适配: NanoBot turn runner + hook 安装 + tool 显式注册
   │
Phase 4  (2-3d) 集成收口: 主仓改造 + 兼容层 + 测试与迁移
```

本版实施计划明确**不纳入 `LightRAG`**。  
文档、代码、接口命名都不为它预留主路径。

---

## Phase 0 — 定边界（1 天）

**目标**：先把产品真相和宿主真相分开，避免后面边做边改。

### Tasks

| # | 任务 | 产出 | 完成标准 |
|---|---|---|---|
| 0.1 | 定 `Wiki` 页面 schema | `04-Wiki-Schema.md` | frontmatter 字段、正文段落、目录规则定稿 |
| 0.2 | 定黑板 schema | `state/board_schema.md` 或同类文档 | 明确 `blackboard.learning` 与 `blackboard.runtime` 分区，以及 `current_task_status / last_observed_ip / active_blind_spots / last_update_ts` 字段 |
| 0.3 | 定模式路由协议 | `state/mode_phase_contract.md` | 明确 `SessionMode = CHAT | LEARNING`，以及 `TurnMode`、`LearningPhase` 的分层关系 |
| 0.4 | 定插件对 NanoBot 的最小宿主接口 | `05-Plugin-Host-Contract.md` | 会话、消息、stream、turn runner、tool registry 五类接口收口 |
| 0.5 | 宿主基线确认 | 确认当前实际接入目标 | 明确首版面对的是当前 `nanobot-core` slim runtime，而不是抽象“未来上游” |

### Deliverables

- 一套不依赖具体实现细节的最小协议
- 明确知识真相、学习状态真相、宿主缓存的边界
- 明确首版不以自动 `entry_points` 发现为强依赖
- 明确黑板既承载学习语义，也承载轻量 runtime 投影，但两者不混写
- 明确插件不是“有时存在有时不存在”，而是依据 `SessionMode` 决定介入深度

### 风险

- 如果这里不定清楚，后面代码会在“旧主线 / 新插件 / 宿主 metadata”之间三头摆动

### Tag

`plugin/phase0-contracts`

---

## Phase 1 — 知识底座与状态地基（2 天）

**目标**：先把课程知识真相层和 session 状态地基做出来，不碰复杂宿主接入。

### Tasks

| # | 任务 | 创建/修改文件 | 完成标准 |
|---|---|---|---|
| 1.1 | 建 `Wiki` 目录结构 | `knowledge/wiki/*` | 至少一条 AI 路径和一条科学路径样例齐全 |
| 1.2 | 写 `Wiki` 页面模板 | `templates/wiki_page.md` 或同类文件 | 概念页、路径页、实验页、题目页模板固定 |
| 1.3 | 实现 `WikiIndexBuilder` | `colearn/colearn_wiki/colearn_indexer.py` | 生成 `wiki_index.json`、`wiki_link_graph.json` |
| 1.4 | 实现 `WikiQueryService` | `colearn/colearn_wiki/colearn_query.py` | 支持 id、alias、关键词、link 扩展查询 |
| 1.5 | 固定 session 状态文件结构 | `colearn/colearn_state/colearn_models.py`, `colearn/colearn_state/colearn_store.py` | `session.json` 默认包含 `blackboard.learning` 与 `blackboard.runtime` |
| 1.6 | 建课程样例集 | `knowledge/wiki/...` | AI/物理各至少 8-12 页可跑通 |
| 1.7 | 测试与抽检 | `tests/test_wiki_index.py`, `tests/test_wiki_query.py`, `tests/test_state_store.py` | 查询结果稳定，状态文件能原子落盘并保留黑板分区 |

### Deliverables

- 本地 Wiki 成为课程知识底座
- 可生成轻量索引与 link graph
- 首版知识检索仅依赖 Wiki 与本地索引
- `session.json` 结构定稿，黑板分区随状态文件一体落盘

### 风险

- 过早把 schema 做太大：首版只保留必须字段
- 内容样例不足：至少准备能支撑一个完整学习闭环的小课程包
- 黑板分区如果此时不定清楚，后面 hooks 和 tool 会反复改字段名

### Tag

`plugin/phase1-wiki`

---

## Phase 2 — 学习内核（3 天）

**目标**：把状态机、黑板、写回链路抽成 CoLearn 插件内核。

### Tasks

| # | 任务 | 创建/修改文件 | 完成标准 |
|---|---|---|---|
| 2.1 | 抽离状态模型 | `colearn/colearn_state/{colearn_models.py, colearn_records.py}` | `LearningSession`、`BoardFacts`、`LearningBoard` 独立可序列化 |
| 2.2 | 实现状态存储 | `colearn/colearn_state/{colearn_store.py, colearn_manager.py, colearn_cache.py}` | session/project 读写、原子写、损坏恢复通过测试 |
| 2.3 | 落地 `SessionMode` 路由 | `colearn/colearn_learning_logic/*` | 能区分 `CHAT` 与 `LEARNING`，并决定是否进入学习主链 |
| 2.4 | 迁移学习内部状态机协议 | `colearn/colearn_learning_logic/*` | 保持 `LEARN / CHECK / PAUSED` 与 phase 分离 |
| 2.5 | 实现黑板写回 | `colearn/colearn_board/colearn_writeback.py` | 一轮结束后能更新 blockers、evidence、continuation |
| 2.6 | 接入 Wiki 查询 | `colearn/colearn_board/colearn_context_builder.py` | 当前节点可拉到先修、误区、例子 |
| 2.7 | 落地黑板 runtime 监控 | `colearn/colearn_hooks/colearn_blackboard_monitors.py` | turn start / end 能更新 `current_task_status`、`last_observed_ip` |
| 2.8 | 落地 `/colearn` 命令 | `colearn/colearn_tools/colearn_command.py` | 命令可直接读取黑板快照，输出 Markdown 与结构化 snapshot |
| 2.9 | parity 与模式测试 | `tests/test_learning_logic_parity.py`, `tests/test_colearn_command.py`, `tests/test_session_mode.py` | 学习逻辑不漂移，`CHAT` 不误入学习主链，`LEARNING` 可稳定写回 |

### Deliverables

- 学习状态不再依赖 NanoBot session metadata 作为唯一真相
- 黑板能围绕 Wiki 节点推进
- 一轮 turn 后的写回逻辑独立成立
- 黑板 runtime 可静默接收宿主运行信号，并可通过 `/colearn` 投影给用户
- 普通闲聊与学习会话有明确分流，不靠每轮临时猜测

### 风险

- 如果把 `TurnMode` 和 `LearningPhase` 再混回去，会重新变重
- parity 测试必须先建，否则迁移过程容易漂移
- 如果 runtime 字段直接塞进 learning 子域，后面会把学习语义污染掉
- 如果不单独引入 `SessionMode`，后面会把“闲聊”和“学习”混在同一套判断里

### Tag

`plugin/phase2-learning-core`

---

## Phase 3 — 宿主适配（2-3 天）

**目标**：把 CoLearn 内核稳定地挂到 NanoBot 上，但把耦合收口在薄适配层。

补充约束：按官方 `NanoBot v0.2.1` 前端现状，当前存在 `chat / settings / apps` 三类宿主视图，`apps` 是可复用入口，但还不是通用页面插件系统。

### Tasks

| # | 任务 | 创建/修改文件 | 完成标准 |
|---|---|---|---|
| 3.1 | 建插件骨架 | `colearn/{__init__.py, colearn_plugin.py, config/*}` | 包可安装，宿主可显式加载 |
| 3.2 | 实现 ContextVar 与 session binder | `colearn/colearn_context.py`, `colearn/colearn_hooks/colearn_session_binder.py` | hooks 与 tools 可通过上下文读取当前 session 黑板 |
| 3.3 | 实现宿主适配层 | `colearn/colearn_adapters/{colearn_session.py,colearn_messages.py,colearn_stream.py,colearn_tool_registry.py,colearn_turn_runner.py}` | 所有宿主调用只从 adapters 进出 |
| 3.4 | 定 UI 扩展契约 | `colearn/colearn_adapters/colearn_ui_extensions.py`, `05-Plugin-Host-Contract.md` | 明确 `apps / page / modal / thread_toolbar` 槽位与 manifest 字段 |
| 3.5 | 实现常驻 hooks | `colearn/colearn_hooks/{colearn_preflight.py,colearn_plan.py,colearn_retrieval.py,colearn_finalize.py,colearn_writeback.py,colearn_blackboard_monitors.py}` | 能读黑板、注入上下文、写回结果，并静默更新 runtime 子域 |
| 3.6 | 先做 tool 显式注册 | `colearn/colearn_plugin.py` | 不依赖自动 `entry_points` 也能注册 `/colearn` |
| 3.7 | 宿主 smoke test | `tests/test_plugin_host_smoke.py` | 当前 slim runtime 下完整 turn 可跑通，`CHAT` 与 `LEARNING` 分流正确，`/colearn` 可读取最新黑板快照 |

### Deliverables

- CoLearn 可以作为插件挂在 NanoBot 上
- 当前 runtime 下不依赖自动插件发现
- 宿主耦合集中在 `adapters/`
- UI 先挂到 `v0.2.1` 已有的 `apps` 入口或固定槽位，不假设已有通用页面插件系统
- `/colearn` 已成为最小黑板可视化入口，后续前端只需消费结构化 snapshot

### 风险

- 当前 `nanobot-core` slim runtime 默认不放开外部 `nanobot.tools` 自动发现，因此首版不应把这条路径当完成标准
- `_loop`、`process_direct`、`_extra_hooks` 仍可能变化，所以必须有 adapter 层
- `apps` 当前是宿主内建入口，不是插件页面注册中心，因此 UI 接入必须补固定契约

### Tag

`plugin/phase3-host-adapter`

---

## Phase 4 — 集成收口（2-3 天）

**目标**：把 CoLearn 主仓切到新插件内核，同时保留必要兼容层，不做激进大拆。

### Tasks

| # | 任务 | 创建/修改文件 | 完成标准 |
|---|---|---|---|
| 4.1 | 主仓启动接入插件 | `colearn/server.py` 等 | server 启动时显式安装 `colearn` 插件 |
| 4.2 | API 层改读插件状态 | `colearn/api/*` | API 通过 `plugin.get_state_manager()` 读写 |
| 4.3 | 接入图谱 UI 入口 | `webui/src/App.tsx`, `webui/src/components/Sidebar.tsx`, 相关 view 组件 | 可从 `apps` 入口或固定按钮打开 CoLearn 图谱 |
| 4.4 | 接入黑板卡片投影 | `webui/src/*` 相关 view 组件 | `/colearn` 的结构化 snapshot 可投影为卡片、弹窗或页面块 |
| 4.5 | 保留兼容 facade | `colearn/app/*`, `colearn/learning/*` 局部 | 旧入口先薄化，不立刻全删 |
| 4.6 | 写迁移脚本 | `colearn/colearn_state/migrations/colearn_from_legacy.py` | 旧状态可迁到新目录结构 |
| 4.7 | 清理旧强耦合点 | `runtime_v2`、旧 nanobot 直连代码 | 只删已经被新链路替代的部分 |
| 4.8 | 集成测试 | `tests/integration/test_full_turn.py` | 一个完整学习 turn 端到端跑通 |

### Deliverables

- 主仓使用新插件链路
- 学习状态与知识真相脱离 NanoBot 内部结构
- 保留薄兼容层，避免一次性推倒过猛
- 图谱入口贴合 `NanoBot v0.2.1` 当前前端结构，可继续往通用槽位演进
- 黑板 snapshot 可被聊天区、Apps 页或右侧卡片统一消费

### 风险

- 不建议一上来就追求 `grep import nanobot = 0`
- 不建议在尚未稳定前删除所有旧 stage 与旧 hooks

### Tag

`plugin/phase4-integrated`

---

## 增强项（后续，不阻断首版）

### A. `nanobot.tools` 自动发现

当前 runtime 如果后续切到完整上游或内部 loader 放开外部 plugin 发现，再增加：

- `entry_points` 注册
- 自动加载验证
- 插件发现失败时的 graceful fallback

这属于增强项，不应作为首版前提。

### B. 通用 UI 插件槽位

当前 `v0.2.1` 可先借 `apps` 入口承载 CoLearn 图谱。  
如果后续宿主要支持更多插件页面，再补：

- `ui_extensions` 清单接口
- `page / modal / thread_toolbar` 固定槽位
- 宿主内建 `graph / panel / timeline` 容器

这属于二阶段增强，不阻断首版上线。

---

## 测试策略

### Unit Tests

- `test_wiki_index.py`
- `test_wiki_query.py`
- `test_state_manager.py`
- `test_state_store.py`
- `test_learning_logic_parity.py`
- `test_context.py`
- `test_hooks_pipeline.py`
- `test_colearn_command.py`
- `test_session_mode.py`
- `test_turn_runner.py`

### Integration Tests

- `test_full_turn.py`
- `test_concurrent_sessions.py`
- `test_plugin_host_smoke.py`

### 教研抽检

至少抽检下面两条学习路径：

1. `机器学习入门`
   - 数据
   - 模式
   - 模型
   - 训练
   - 简单检查题

2. `力与运动入门`
   - 力
   - 重力
   - 运动状态
   - 常见误区
   - 一个实验活动

抽检标准：

- 解释是否适龄
- 先修关系是否自然
- 黑板更新是否符合学习节奏
- blocker 是否能被正确记录

---

## 回滚计划

### 分阶段 tag

- `plugin/phase0-contracts`
- `plugin/phase1-wiki`
- `plugin/phase2-learning-core`
- `plugin/phase3-host-adapter`
- `plugin/phase4-integrated`

### 回滚原则

1. 协议层有问题，回到 `phase0`
2. Wiki 层有问题，回到 `phase1`
3. 学习逻辑漂移，回到 `phase2`
4. 宿主接入不稳，回到 `phase3`

### 数据回滚

- 新增 `.colearn/wiki/` 和 `.colearn/state/` 独立目录
- 迁移旧数据时保留只读备份
- 迁移脚本必须支持 `--dry-run`

---

## 关键决策

### D1：知识底座选什么？

→ **本地 Wiki 文件**

### D2：首版是否包含 LightRAG？

→ **不包含**。首版方案里直接去掉

### D3：状态机和黑板放哪？

→ **放 CoLearn 插件内核**

### D4：NanoBot 承担什么？

→ **宿主与执行层**

### D5：首版是否强依赖自动插件发现？

→ **不强依赖**。首版以显式注册和薄适配层为主

### D6：核心目标是什么？

→ **不是零耦合，而是把耦合收口到薄适配层**

### D7：图谱 UI 首版挂在哪？

→ **优先挂在 `NanoBot v0.2.1` 已有的 `apps` 入口，或通过固定按钮打开弹窗**

### D8：黑板 runtime 首版怎么落？

→ **作为 `blackboard.runtime` 子域写进 `session.json`，通过 hooks 静默更新，并由 `/colearn` 先行投影**

### D9：闲聊和学习怎么区分？

→ **引入 `SessionMode = CHAT | LEARNING` 作为最上层路由；`TurnMode` 只在 `LEARNING` 内部生效**

---

## 时间表汇总

| Phase | 工作量 | 风险 | 里程碑 |
|---|---|---|---|
| 0. 定边界 | 1 天 | 中 | 协议定稿 |
| 1. 知识底座 | 2 天 | 低 | Wiki 可查可索引 |
| 2. 学习内核 | 3 天 | 高 | 状态机 + 黑板独立成立 |
| 3. 宿主适配 | 2-3 天 | 中 | 插件可挂到 NanoBot |
| 4. 集成收口 | 2-3 天 | 中 | 主仓走新链路 |
| **合计** | **11-14 天** | — | 首版插件化落地 |

---

## 接下来

通过后，从 **Phase 0** 开始，不直接写 NanoBot 适配代码。  
先把 `Wiki schema`、`黑板 schema`、`宿主接口` 三件事定死，再推进实现。
