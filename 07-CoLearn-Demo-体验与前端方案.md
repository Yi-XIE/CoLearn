# CoLearn Demo 体验与前端方案
> 版本：v0.1  
> 日期：2026-06-02  
> 状态：基于当前 Demo 路径冻结  
> 适用范围：CoLearn Demo 完整版

## 0. 文档目标

本文用于定义 CoLearn Demo 版的产品体验路径与前端呈现方案，重点回答以下问题：

- 用户从哪里进入 CoLearn
- 学习模式如何在聊天中被触发
- 学习规划卡片如何生成与展示
- NanoBot 现有前端能力可以复用到什么程度
- CoLearn 页面应如何放置在当前线程体验中

本文只覆盖 Demo 完整版范围，不定义完整产品版教学系统。

---

## 1. 设计结论

### 1.1 主路径采用 chat-first

CoLearn 的第一入口应放在聊天界面，而不是 Apps 页面。

用户的自然心智是：

- 我想开始系统学习一件事
- 我希望系统帮我明确目标和规划路径
- 我希望学习状态可以持续被维护

因此，CoLearn 应表现为一种进入持续学习状态的对话能力，而不是需要先打开的独立应用。

### 1.2 Apps 不是主入口

NanoBot 当前的 `Apps` 更接近能力目录、安装页和集成页，主要承载：

- CLI Apps
- MCP Presets

因此，CoLearn 不应把 Apps 作为主入口。Apps 中的 CoLearn 只承担以下角色：

- 全局发现入口
- 演示入口
- 次级回看入口

### 1.3 当前线程内需要一个正式的 CoLearn 查看入口

进入学习模式后，用户应能在当前线程内直接查看学习状态，而不需要离开聊天主界面。

本方案采用两层线程内入口：

1. `goal strip` 的局部展开 panel
2. 基于 NanoBot `Sheet` 组件的右侧 CoLearn panel

### 1.4 学习规划卡片的职责是完成模式切换

学习规划卡片不是课程大纲，也不是系统状态页。

它的任务是帮助用户完成从普通聊天到学习模式的切换，并回答三件事：

- 我要学什么
- 系统准备怎么带我学
- 我现在先从哪里开始

---

## 2. 用户主链路

## 2.1 触发入口

推荐保留三种入口，但职责不同：

- 自然语言触发  
  例如：`我想系统学习机器学习`

- `/learn`  
  正式进入或切换到 CoLearn 学习模式

- `/colearn`  
  查看当前学习状态，不承担主入口职责

## 2.2 主流程

### 步骤 1：在聊天中触发学习意图

用户通过自然语言或 `/learn` 表达想长期推进一个学习目标。

### 步骤 2：系统索要目标

系统先收敛最关键的两类信息：

- 想学什么
- 想学到什么程度，或想解决什么问题

第一轮不追求收集完整用户画像，只追求能够生成一版可启动的学习路线。

### 步骤 3：系统生成学习规划卡片

系统输出一张学习规划卡片，明确：

- 当前学习目标
- 建议学习路径
- 当前起点
- 第一轮安排

### 步骤 4：确认进入 LEARNING 模式

系统明确告诉用户已经进入 CoLearn 学习模式，并建立本轮学习状态。

### 步骤 5：继续在聊天中推进学习

后续每轮对话围绕该学习目标持续推进，系统更新学习状态。

### 步骤 6：在需要时查看状态

用户可通过以下入口查看当前学习状态：

- `goal strip` 局部展开 panel
- 线程内右侧 CoLearn panel
- `/colearn`
- Apps 中的 CoLearn 条目

---

## 3. 对话策略

## 3.1 触发时

系统不应像填写表单，而应像进入一个新的陪学状态。

推荐表达方向：

- 你想把这件事作为一个持续学习目标来推进
- 我先帮你明确目标，再给你一条学习路径
- 我们现在开始

## 3.2 索要目标时

第一轮只问最关键的问题，不展开过多分支。

推荐先问：

- 你最想系统学会的是什么
- 你更希望快速入门、系统掌握，还是围绕具体任务尽快上手

## 3.3 确认进入模式时

必须有一句清晰的话告诉用户现在已经进入学习模式。

推荐表达方向：

- 你现在已经进入 CoLearn 学习模式
- 当前目标是什么
- 当前起点是什么
- 下一步我们先做什么

---

## 4. 学习规划卡片

## 4.1 卡片定位

学习规划卡片是一张轻决策卡片，不是完整课程大纲，也不是系统状态页。

它需要帮助用户确认以下三件事：

- 学习目标是什么
- 路径怎么走
- 现在先做什么

## 4.2 卡片结构

推荐固定为五个区块：

### 1）卡片头

- 标题：`CoLearn 学习规划`
- 副标题：基于当前目标生成的第一版学习路线
- 状态：`待确认` 或 `初版`

### 2）目标定义

- 学习主题
- 目标深度
- 应用场景

### 3）学习路径

建议只展示三个阶段，每个阶段只说明：

- 这一阶段学什么
- 学完后能获得什么

### 4）当前起点

- 当前建议起点
- 为什么从这里开始
- 第一轮安排

### 5）下一步

- 如果认可这条路径，就开始第一轮学习
- 如果不认可，可以调整目标、节奏或起点

## 4.3 卡片阅读顺序

卡片应按以下顺序被理解：

1. 目标是什么
2. 路线怎么走
3. 现在先做什么

## 4.4 推荐 Markdown 形态

```md
# CoLearn 学习规划
基于你当前目标生成的第一版学习路线
待确认

## 目标定义
- 学习主题：机器学习
- 目标深度：建立系统认知，并能开始做入门项目
- 应用场景：用于 AI 产品设计与原型验证

## 学习路径
1. 建立基础框架
理解机器学习的核心任务、基本流程和常见问题类型

2. 掌握核心方法
学习监督学习、训练、评估与常见建模思路

3. 结合真实任务应用
围绕一个小项目把知识转成实践能力

## 当前起点
- 当前建议起点：监督学习
- 为什么从这里开始：它最容易建立完整学习闭环
- 第一轮安排：先理解监督学习的基本流程，并用一个简单例子建立直觉

## 下一步
如果你认可这条路径，我们就按这个起点开始第一轮学习。
如果你想调整目标、节奏或起点，我可以先帮你改一版。
```

---

## 5. 前端总体结构

本次前端设计采用三层展示：

### 第一层：聊天中的 Markdown 学习规划卡片

这是主展示面，承载完整的学习规划内容。

### 第二层：聊天内学习状态入口

包括：

- `goal strip`
- `goal strip` 局部展开 panel
- 线程内右侧 CoLearn panel

### 第三层：Apps 中的 CoLearn

作为全局发现与演示入口，不承担主路径职责。

---

## 6. NanoBot 现有能力复用结论

## 6.1 Markdown 文本渲染

NanoBot WebUI 已支持 Markdown 文本渲染，因此学习规划卡片可以直接作为 assistant 消息输出。

这意味着本轮不需要额外定义专用卡片组件，也不需要先实现复杂的结构化消息协议。

## 6.2 goal strip 轻状态层

NanoBot 当前已有 sustained goal 的轻状态层：

- 输入区上方的 `goal strip`
- 点击后展开的局部 panel

这套能力适合承载：

- 当前是否处于学习模式
- 当前学习目标短摘要
- 当前学习目标的简版说明

## 6.3 Sheet 右侧抽屉

NanoBot 当前已有通用 `Sheet` 组件，支持 `side="right"`。

因此，线程内右侧 CoLearn panel 可以基于现有组件实现，不需要从零新造抽屉基础设施。

---

## 7. goal strip 方案

## 7.1 strip 的角色

进入学习模式后，输入区上方继续显示一条学习状态条。

它的任务是：

- 持续表达当前仍处于学习模式
- 提供轻查看入口
- 提供完整查看入口

## 7.2 strip 展示内容

建议展示短摘要，不展示完整状态字段。

例如：

- `目标 · 机器学习学习路径`
- `目标 · 监督学习基础阶段`

## 7.3 strip 右侧动作

建议在 strip 右侧放两个动作：

### 动作 1：展开摘要

复用当前已有的局部 panel 展开能力，用于快速查看：

- 当前目标
- 当前起点
- 当前阶段
- 下一步一句话建议

### 动作 2：打开 CoLearn

新增一个明确按钮，点击后打开线程内右侧 CoLearn panel。

因此，strip 不只是状态提示，而是一个学习状态入口带。

---

## 8. goal strip 局部展开 panel

## 8.1 角色定位

这是一个轻量信息层，不是完整 CoLearn 页面。

它只回答：

- 当前目标是什么
- 当前这一轮在学什么
- 当前路径大致是什么

## 8.2 展示内容

建议只放 Markdown 摘要，不放复杂布局。

推荐内容：

- 当前学习目标
- 当前起点
- 当前阶段
- 下一步建议

## 8.3 不承担的职责

这里不展示：

- 完整黑板
- 图谱详情
- 多区块状态面板
- 节点关系全量结构

这些内容统一交给右侧 CoLearn panel。

---

## 9. 线程内右侧 CoLearn panel

## 9.1 入口位置

入口放在 `ThreadComposer` 区域，与 `goal strip` 同层。

原因：

- `goalState` 已经在这一层消费
- 当前学习状态条本来就在这里展示
- 用户在继续发消息前最容易看到这里
- 这是最贴近当前学习线程上下文的位置

## 9.2 触发方式

由 `goal strip` 右侧的 `打开 CoLearn` 按钮触发。

推荐形式：

- 文本按钮：`CoLearn`
- 或图标按钮 + tooltip：`打开 CoLearn`

## 9.3 面板定位

右侧 CoLearn panel 是当前学习线程的主只读状态面板。

它回答的问题是：

- 我现在在学什么
- 我学到哪里了
- 当前聚焦的知识点是什么
- 接下来建议做什么

## 9.4 展示形式

使用 NanoBot 现有 `Sheet` 组件，`side="right"`。

该面板不替换聊天主界面，不切走当前线程，而是作为线程内上下文页面存在。

---

## 10. 右侧 CoLearn panel 内部布局

建议自上而下分成三段：

## 10.1 顶部：学习摘要区

形式：Markdown 摘要卡

内容：

- 当前目标
- 当前阶段
- 当前起点
- 下一步建议

这一块优先保证“可读、像产品”，而不是字段堆砌。

## 10.2 中部：黑板区

形式：结构化只读区块

建议字段：

- 当前进度
- continuation / 下一步
- 待检查项
- 阻塞项
- objections / evidence_refs（如有）

这一块负责表达系统内部学习状态。

## 10.3 底部：图谱区

形式：只读焦点图谱

第一版建议只支持：

- 当前焦点节点
- 相邻节点
- 简单路径关系

本轮不做重交互拖拽，不做图谱编辑，不做复杂布局操作。

---

## 11. Apps 与导航定位

## 11.1 Apps 的当前语义

NanoBot 当前的 `Apps` 更接近：

- 能力目录
- 安装页
- 启用页
- 集成配置页

因此，Apps 并不等同于“当前线程功能页”。

## 11.2 本轮定位

本轮明确：

- CoLearn 的主入口在聊天线程内
- Apps 中的 CoLearn 是次入口

Apps 中的 CoLearn 适合承担：

- 全局发现
- 演示入口
- 非当前线程场景下的查看入口

## 11.3 后续导航建议

如果后续宿主允许新增独立导航对象，推荐把 `CoLearn` 作为独立入口放在导航栏中，并放在 `Apps` 下方，而不是被归入 `Apps` 目录语义内部。

---

## 12. 字段映射建议

## 12.1 学习规划卡片

建议先做最小映射：

- `goal` <- 目标定义
- `planned_nodes` <- 学习路径中的阶段或节点
- `active_node_id` <- 当前起点
- `continuation` <- 第一轮安排 / 下一步

## 12.2 goal strip

建议映射为：

- `goal_state.ui_summary` <- 学习目标短摘要
- `goal_state.objective` <- 学习目标完整描述

## 12.3 右侧 CoLearn panel

建议读取：

- 当前学习摘要
- Blackboard 当前快照
- Graph 当前快照

数据面应与 CoLearn 只读 API 保持一致。

---

## 13. Demo 成功标准

当用户首次体验结束后，他能够自然说出以下内容：

- 我知道怎么在聊天里开始一个学习目标
- 我知道系统会先帮我明确目标并生成学习路线
- 我知道当前学习状态会被持续维护
- 我知道 goal strip 只能快速看摘要
- 我知道点按钮可以打开完整的 CoLearn 状态面板
- 我知道 Apps 里的 CoLearn 是次入口，不是主路径入口

如果以上几点成立，则本次 CoLearn Demo 的体验主链路和前端结构成立。
