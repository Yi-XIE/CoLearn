# CoLearn Demo 体验与前端方案

版本：v0.2 Demo  
日期：2026-06-04  
状态：按当前实现更新

## 0. 文档目标

这份文档用于定义 CoLearn Demo 的真实前端体验路径，重点回答：

- 用户从哪里进入 CoLearn
- 学习模式如何在聊天里触发
- 学习状态如何在当前线程内查看
- Apps 页在这套体验中的职责是什么

本文只覆盖 Demo 完整版，不定义完整产品版教学系统。

## 1. 设计结论

### 1.1 主路径是 chat-first

CoLearn 的主入口放在聊天线程内，不放在 Apps 页里。

用户的自然预期是：

- 我想开始一个持续学习目标
- 我想在当前对话里推进这个目标
- 我想不用离开聊天界面，就能看到学习状态

所以 CoLearn 在前端上表现为“聊天中的学习模式”，不是“需要先打开的独立应用”。

### 1.2 Apps 不是主入口

NanoBot 当前的 `Apps` 更接近：

- 能力目录
- 展示入口
- 次级查看入口

因此 CoLearn 在 Apps 里存在，但不承担主路径职责。

### 1.3 当前线程页需要正式 CoLearn 入口

进入学习模式后，用户应当能在当前线程内直接查看学习状态。

本轮采用两层线程内入口：

1. 输入区附近的 `CoLearn` 按钮
2. 打开后的线程内右侧并排只读栏

## 2. 用户主链路

### 2.1 触发入口

当前推荐保留三种入口，但职责不同：

- 自然语言触发
- `/learn`
- `/colearn`

其中：

- `/learn` 是正式学习入口
- `/colearn` 是只读查看入口

### 2.2 主流程

1. 用户在聊天中表达学习目标，或直接输入 `/learn <goal>`
2. 系统创建或继续当前学习 session
3. 系统明确进入 `LEARNING` 模式
4. 后续学习对话继续在当前线程内推进
5. 用户通过 `CoLearn` 按钮打开右侧只读栏查看状态

## 3. 前端入口布局

### 3.1 线程页主入口

CoLearn 主入口位于 `ThreadComposer` 附近，与当前消息输入路径保持同屏。

原因：

- 用户继续发消息前最容易看到这里
- 当前线程上下文已经在这一层
- 这里最适合作为“继续学习”和“查看学习状态”的结合点

### 3.2 右侧栏形态

CoLearn 打开后不是覆盖式抽屉，而是线程页右侧并排栏。

当前形态：

- 左侧聊天区约 `60%`
- 右侧 CoLearn 栏约 `40%`
- 打开后会把中间聊天区挤向左侧
- 仍保持同屏聊天和同屏查看

这符合“当前线程里的工作侧栏”心智，不是“临时遮罩层”心智。

### 3.3 Apps 页职责

Apps 页中的 CoLearn 只承担：

- 全局发现
- 演示入口
- 次级查看入口

不承担主交互路径。

## 4. 右侧 CoLearn 栏内容

当前右侧栏为只读状态面板，承载三类信息：

### 4.1 学习摘要

- 当前目标
- 当前模式
- 当前 active node

### 4.2 Blackboard 摘要

- 当前 progress
- pending checks
- blockers

### 4.3 图谱摘要

- graph 节点数量
- graph 边数量
- 当前 focus 相关信息

本轮不做：

- 图谱编辑
- 拖拽交互
- 多面板联动编辑

## 5. 与 Markdown 学习卡片的关系

学习规划卡片仍然可以作为聊天消息输出。

也就是说，当前前端分成两层：

1. 聊天消息里的学习规划与讲解
2. 右侧栏里的只读学习状态

这样用户既能在消息流里看到教学内容，也能在侧栏里看到当前学习状态。

## 6. 当前实现结论

截至当前版本，前端侧已达成：

- 聊天线程页有 CoLearn 主入口
- CoLearn 不再是覆盖式右抽屉
- CoLearn 以同屏右栏方式显示
- 右栏打开后会挤压中栏
- Apps 页仍保留 CoLearn 次入口

## 7. Demo 成功标准

用户首次体验后，应该能自然说出这些结论：

1. 我知道怎么在聊天里开始学习目标
2. 我知道 `/learn` 是正式入口
3. 我知道 CoLearn 可以在当前线程页里同屏打开
4. 我知道右侧栏是查看学习状态的地方
5. 我知道 Apps 里的 CoLearn 只是次入口，不是主入口
