# CoLearn 插件化技术设计文档

> 版本：v1.2  
> 日期：2026-06-01  
> 状态：按 `Wiki + 状态机 + 黑板 + NanoBot 薄适配层` 方案重写  
> 适用对象：K12 STEAM 陪学产品（10-15 岁、可扩展到 10-18 岁）

## 0. 文档导航

- 本文档：总体架构、边界、数据模型、宿主适配策略
- [02-实施计划.md](./02-实施计划.md)：分阶段实施计划与里程碑
- 后续补充文档：
  - `03-迁移指南.md`：旧数据与旧链路迁移
  - `04-Wiki-Schema.md`：课程 Wiki 页面规范
  - `05-Plugin-Host-Contract.md`：插件对 NanoBot 的最小宿主接口

### 0.1 命名规范

本方案后续代码与状态目录统一采用以下命名：

- Brand Name: `CoLearn`
- Package Name: `colearn`
- Plugin Name: `colearn`
- 新增代码文件 / 目录前缀：`colearn_*`
- State Dir: `.colearn/`

说明：

- 编号型设计文档继续沿用现有命名，如 `01-技术设计.md`
- 既有主仓路径如 `colearn/server.py`、`colearn/api/*` 不强制改名
- 本文档中新增插件专属代码路径，统一按 `colearn` 包名和 `colearn_*` 前缀书写

---

## 1. 设计结论

本次方案明确**去掉 `LightRAG` 层**，也不再追求“把整个 CoLearn 主链塞进 NanoBot 插件系统”。

新的核心结论是：

1. **知识真相层**：采用本地 `Wiki` 文件，作为课程知识的唯一可审计来源
2. **学习控制层**：采用 `状态机 + 黑板`，作为 CoLearn 的产品内核
3. **宿主执行层**：采用 `NanoBot`，负责 turn loop、tool 调度、stream、session
4. **接入方式**：CoLearn 以插件形式挂在 NanoBot 上，但只暴露一层**薄适配层**
换句话说：

```text
Wiki = 知识真相
状态机 + 黑板 = 学习推进真相
NanoBot = 宿主与执行引擎
插件 = CoLearn 接 NanoBot 的薄桥
```

---

## 2. 为什么这样选

### 2.1 面向 K12 场景的真实需求

目标用户是学习人工智能、科学和工程基础知识的学生，不是研究型用户。首版系统最重要的不是“开放世界检索能力最强”，而是下面四件事：

1. 课程知识边界清楚
2. 先修关系稳定
3. 常见误区和解释口径可控
4. 教研可审阅、可回滚、可逐页修改

`LLM Wiki` 更像可维护的课程知识底座，更符合 K12 场景的首版需要。

### 2.2 响应时间与投入的工程判断

在当前产品范围内，采用 `Wiki + 本地索引`，知识层开销更小，调试面也更窄。

经验估算如下：

| 方案 | 知识层额外开销 | 常见整轮响应时间 | 首版研发投入 | 运维复杂度 |
|---|---|---|---|---|
| `Wiki + 本地索引` 作为核心 | `50-300ms` | `1.1-3.3s` | 中低 | 低 |

说明：

- 以上是当前架构下的工程估算，不是正式 benchmark
- 不把模型本身差异算入，只比较知识层与编排层的额外开销
- 对首版而言，响应时间不是唯一指标，**可控性和维护成本更关键**

### 2.3 对 CoLearn 当前架构的影响

CoLearn 现有主线已经有自己的：

- 学习状态协议
- 黑板计划
- `LEARN / CHECK / PAUSED` 三态
- `LearningPhase`
- 会话级写回与前端展示

这些是产品内核，不应因为改成插件而交给 NanoBot 负责。  
插件化应当解决的是**宿主耦合问题**，不是让 CoLearn 放弃自己的学习语义。

---

## 3. 总体架构

### 3.1 三层模型

```text
+---------------------------------------------------+
|                   CoLearn Product                 |
+---------------------------------------------------+
|  1. 知识真相层：Wiki                              |
|     - 课程概念页                                   |
|     - 先修关系                                     |
|     - 误区 / 实验 / 例题 / 证据来源                |
+---------------------------------------------------+
|  2. 学习控制层：CoLearn Plugin Core               |
|     - 状态机                                       |
|     - 黑板                                         |
|     - 学习计划                                     |
|     - 事件抽取 / 写回                              |
+---------------------------------------------------+
|  3. 宿主执行层：NanoBot                           |
|     - turn loop                                    |
|     - tool registry                                |
|     - session / stream / model                     |
+---------------------------------------------------+
```

### 3.2 边界原则

1. **Wiki 不依赖 NanoBot**
2. **状态机和黑板不依赖 NanoBot 内部数据结构**
3. **CoLearn 插件只依赖 NanoBot 的最小宿主接口**
4. **NanoBot session 不是学习真相，只允许做镜像或短期缓存**
5. **即使未来更换宿主，Wiki 与学习状态也能保留**

---

## 4. 知识真相层：Wiki

### 4.1 定位

Wiki 是课程知识底座，不是聊天记录，也不是检索缓存。

它承载的是：

- 概念定义
- 年龄段适配解释
- 先修关系
- 常见误区
- 实验 / 观察 / 动手任务
- 例题与练习
- 来源引用

### 4.2 页面组织建议

```text
knowledge/
  wiki/
    concepts/
      what-is-a-model.md
      what-is-training-data.md
      gravity.md
    paths/
      ml-foundation-path.md
      mechanics-foundation-path.md
    experiments/
      pendulum-observation.md
      paper-airplane-lift.md
    question-banks/
      ml-beginner-checks.md
      force-and-motion-checks.md
    glossary/
      terms.md
  generated/
    wiki_index.json
    wiki_link_graph.json
    wiki_search_index.json
```

### 4.3 页面 schema

每个概念页采用 Markdown + frontmatter。

建议字段：

```yaml
id: ml.model.basic
title: 什么是模型
aliases:
  - 模型
grade_band: [10-12, 13-15]
domain: ai
difficulty: beginner
prerequisites:
  - ml.data.basic
  - ml.pattern.basic
learning_objectives:
  - 知道模型是“从例子里总结规律”的工具
misconceptions:
  - 模型不是“把答案背下来”
experiment_refs:
  - exp.sorting-rules-cards
question_refs:
  - qb.ml.beginner.01
source_refs:
  - source/course-ai-week1
updated_at: 2026-06-01
```

正文统一保留这些段落：

- `## 适合学生的解释`
- `## 先修知识`
- `## 常见误区`
- `## 例子`
- `## 动手活动`
- `## 进阶连接`

### 4.4 查询策略

MVP 不把 Wiki 查询设计成复杂 RAG 系统，而是本地结构化查询：

1. frontmatter 过滤
2. 标题 / alias / 关键词检索
3. link graph 扩展
4. 必要时补充局部全文搜索

MVP 不依赖外部向量库，也不设计任何 RAG 派生层。

---

## 5. 学习控制层：状态机与黑板

### 5.1 定位

这一层是 CoLearn 真正的产品资产。

它负责：

- 当前学生在学什么
- 卡在哪
- 下一步该讲什么
- 哪些概念已经掌握
- 哪些证据支持当前判断

NanoBot 只负责跑一轮对话，不负责定义这些学习语义。

### 5.2 会话模式路由

在 `TurnMode` 之前，还需要补一个更高层的模式判断：

- `SessionMode = CHAT | LEARNING`

职责划分如下：

- `SessionMode`：决定当前 session 是否启用 CoLearn 学习主链
- `TurnMode`：只在 `LEARNING` 模式内部生效，决定这一轮如何推进
- `LearningPhase`：描述学习流程所处阶段

也就是说：

```text
SessionMode
  -> CHAT
  -> LEARNING
       -> TurnMode
            -> LEARN / CHECK / PAUSED
       -> LearningPhase
            -> INTAKE / DIAGNOSE / READY / REFLECT / RECALL
```

这个分层用来回答一个非常实际的问题：

- 什么时候只是普通闲聊
- 什么时候要进入学习模式并调用 CoLearn 主链

### 5.3 学习内部状态模型

沿用当前主线里已经相对稳定的做法：

- `TurnMode`：`LEARN / CHECK / PAUSED`
- `LearningPhase`：`INTAKE / DIAGNOSE / READY / REFLECT / RECALL`

这里不把 `PLAN`、`REFLECT` 重新塞回 `TurnMode`。  
计划、收尾和回访属于 phase，不属于单轮执行模式。

### 5.4 模式切换规则

首版建议按下面优先级判断 `SessionMode`：

1. **显式触发优先**
   - 点击“开始学习”
   - 从 `Apps` 进入 CoLearn
   - 输入 `/learn`、`/colearn`
   - 明确提出“带我系统学”“帮我做学习计划”“先测一下我会不会”

2. **session 粘性**
   - 已经进入 `LEARNING` 的 session，后续默认保持学习模式
   - 不每轮重新猜测

3. **普通问答默认留在 `CHAT`**
   - 单次概念解释
   - 临时问一个知识点
   - 不带进度、计划、练习、检查诉求的轻量问答

4. **模糊情况只建议，不强切**
   - 系统可提示“要不要切到 CoLearn 学习模式”
   - 但不自动把当前 session 强行切成 `LEARNING`

### 5.5 两种模式下的插件工作深度

插件不是“有时存在、有时不存在”，而是始终挂在宿主上，只是介入深度不同。

`CHAT` 模式下：

- 不加载 `blackboard.learning`
- 不跑学习状态机主链
- 不做 Wiki 主链注入
- 只保留轻量 runtime 监控
- `/colearn` 入口仍然可用

`LEARNING` 模式下：

- 加载 `blackboard.learning`
- 查询 Wiki 当前节点与先修关系
- 注入学习上下文
- 运行 `LEARN / CHECK / PAUSED`
- 写回学习状态与 runtime 状态

### 5.6 黑板模型

黑板不建议做成一层散乱字段，而应固定拆成两个稳定子域：

- `blackboard.learning`
- `blackboard.runtime`

其中：

- `learning` 负责学习推进语义
- `runtime` 负责静默采集宿主运行信号，服务 `/colearn` 看板与后续前端卡片投影

`blackboard.learning` 至少包含：

- `goal`
- `active_node_id`
- `current_progress`
- `planned_nodes`
- `completed_nodes`
- `blockers`
- `objections`
- `evidence_refs`
- `continuation`
- `pending_checks`
- `recall_plan`

`blackboard.runtime` 首版建议包含：

- `current_task_status`
- `last_observed_ip`
- `active_blind_spots`
- `last_update_ts`

对应的 session 状态样例：

```json
{
  "session_id": "sid_2026_xxxx",
  "profile": {},
  "board_facts": {},
  "blackboard": {
    "learning": {
      "goal": null,
      "active_node_id": null,
      "blockers": [],
      "evidence_refs": []
    },
    "runtime": {
      "current_task_status": "RUNNING",
      "last_observed_ip": "127.0.0.1",
      "active_blind_spots": [],
      "last_update_ts": 1780281600
    }
  }
}
```

### 5.7 生命周期与持久化

黑板的真相位置仍然是 session 状态文件，不是 `ContextVar` 本身。

首版建议：

- 每个 session 维护一个 `session.json`
- `blackboard` 作为 session 状态的一级字段持久化
- `ContextVar` 只负责把当前 turn 绑定到当前 session 引用
- turn 结束、报错、关键状态切换后统一 flush 到文件

这样可以兼顾：

- 首版足够轻
- 教研与开发可直接审阅状态
- 不需要引入 Redis

### 5.8 黑板上下文接口

在 `colearn/colearn_context.py` 中，建议封装最小黑板访问接口：

```python
from typing import Any
import time

def get_blackboard(domain: str | None = None) -> dict:
    tc = get_turn_context()
    if not tc or not hasattr(tc, "session"):
        return {}
    board = tc.session.state.setdefault("blackboard", {})
    if domain is None:
        return board
    return board.setdefault(domain, {})

def update_blackboard(domain: str, key: str, value: Any) -> None:
    tc = get_turn_context()
    if not tc or not hasattr(tc, "session"):
        return
    board = tc.session.state.setdefault("blackboard", {})
    bucket = board.setdefault(domain, {})
    bucket[key] = value
    if domain == "runtime":
        bucket["last_update_ts"] = int(time.time())
```

这里要强调：

- `ContextVar` 是当前 turn 的访问通道
- `session.json` 才是持久化真相
- 首版按单进程原子写文件设计，不引入额外共享缓存层

### 5.9 学习事件模型

每轮结束后，插件从用户消息、模型回答、tool 结果中抽取学习事件：

- `NODE_STARTED`
- `NODE_COMPLETED`
- `BLOCKER_FOUND`
- `BLOCKER_RESOLVED`
- `MISCONCEPTION_DETECTED`
- `EVIDENCE_ATTACHED`
- `CHECK_REQUESTED`
- `REFLECT_REQUESTED`

事件只更新 CoLearn 自己的黑板，不要求 NanoBot 认识这些语义。

### 5.10 静默监听与斜杠看板

为了支撑“静默增强 + 斜杠看板”这条产品体验，首版建议补两类轻量能力：

1. `blackboard_monitors`
2. `/colearn` 命令投影

`blackboard_monitors` 在 turn 开始和结束时静默更新 `blackboard.runtime`：

- turn start:
  - `current_task_status = RUNNING`
  - 写入 `last_observed_ip`
- turn end:
  - 成功写 `SUCCESS`
  - 失败写 `FAILED`

`/colearn` 命令不触发复杂知识链路，只读取黑板快照并输出轻量看板。  
为了兼容后续 `apps` 入口、弹窗和页面，建议它同时保留两种输出：

- 结构化 snapshot
- Markdown 卡片投影

建议的结构化返回形态：

```json
{
  "type": "blackboard_snapshot",
  "learning": {},
  "runtime": {
    "current_task_status": "RUNNING",
    "last_observed_ip": "127.0.0.1",
    "active_blind_spots": []
  },
  "render_hint": "card"
}
```

---

## 6. CoLearn 插件对 NanoBot 的边界

### 6.1 目标

插件化的目标不是“零耦合”，而是：

**把宿主耦合压缩到一层薄适配层。**

### 6.2 插件职责

CoLearn 插件负责：

- 加载 session 黑板
- 从 Wiki 找本轮相关概念
- 计算本轮教学策略
- 注入 system context
- 静默监听宿主运行状态并更新 `blackboard.runtime`
- 将流式事件转成 CoLearn 可消费的事件
- 对 `/colearn` 输出黑板投影
- 回合结束后更新黑板与状态

### 6.3 NanoBot 宿主职责

NanoBot 负责：

- 承载 turn loop
- 调模型
- 调 tools
- 保持 session
- 发出 stream

### 6.4 最小宿主接口

插件只依赖以下最小接口：

1. `resolve_session_id(ctx)`
2. `read_messages(ctx)`
3. `inject_system_context(ctx, payload)`
4. `run_turn(prompt, session_key, on_stream, on_stream_end)`
5. `tool_registry.register/unregister/get`
6. `ui_extensions.list(slot) / ui_extensions.resolve(extension_id)`

只要这六类能力还能接上，插件就能随着 NanoBot 升级重新接回去。

---

## 7. 适配层设计

### 7.1 真实情况

当前 NanoBot 并没有一个完整公开的“plugins 主接口”。  
而且 CoLearn 当前 vendored slim runtime 对外部 `nanobot.tools` 自动发现也不是默认放开的。

因此首版不能把“entry_points 自动发现”当成唯一路径。

### 7.2 MVP 的接入原则

MVP 采用：

- **常驻 hook 显式安装**
- **Tool 显式注册为主**
- `entry_points` 作为后续增强，不作为首版唯一依赖

这比“强依赖自动发现”更符合当前实际 runtime。

### 7.3 适配层文件

```text
colearn/
  colearn_adapters/
    colearn_host_contract.py
    colearn_session.py
    colearn_messages.py
    colearn_stream.py
    colearn_tool_registry.py
    colearn_turn_runner.py
    colearn_ui_extensions.py
  colearn_hooks/
    colearn_blackboard_monitors.py
  colearn_tools/
    colearn_command.py
```

### 7.4 TurnRunner

保持当前判断：  
不把 `Nanobot.run(hooks=...)` 当作主入口，而是封装一层 `TurnRunner`，统一管理 turn 执行、session_key 和 stream 回调。

这样做的目标不是追求“最官方”，而是把风险收口在一个文件里。

### 7.5 前端扩展策略

按官方 `NanoBot v0.2.1` 前端结构看，宿主前端已经有页面切换壳，但当前壳体是：

- `chat`
- `settings`
- `apps`

其中 `apps` 入口已经挂在侧边栏上，但它当前本质上仍然是宿主内建视图，不是插件可自由注册的新页面系统。  
也就是说，**前端具备新增页面的工程能力，但还没有形成公开稳定的页面级插件协议**。

但这里有一个边界必须写清楚：

- **当前前端能加页面**
- **当前前端没有公开稳定的“页面级插件注入接口”**

所以 CoLearn 后续如果要把图谱做成按钮、弹窗或独立页面，不能假设插件自己往前端里塞一段任意 React 代码就能长期可用。  
更不能把 `v0.2.1` 里的 `apps` 误判成“已经支持插件新增页面”。

更稳的做法是：

- NanoBot 宿主前端一次性补一层**固定扩展槽位**
- CoLearn 插件只声明“我要占哪个槽位、展示什么数据、使用哪种内建容器”
- 宿主前端负责把声明渲染成页面、弹窗或工具栏入口

### 7.6 扩展槽位

建议宿主先定义四类固定槽位：

1. `sidebar`
2. `thread_toolbar`
3. `modal`
4. `page`

CoLearn 首版图谱能力建议先占两个槽位：

- `thread_toolbar`：提供“知识图谱”按钮
- `page` 或 `modal`：展示图谱详情

这样可以先做轻量弹窗，再视产品需要升级成独立页面，而不改插件主逻辑。

如果想尽量贴近 `v0.2.1` 当前结构，首版也可以采用一个更保守的落法：

- 先在宿主里新增 `apps` 下的一个 `CoLearn` 分组入口
- 点击后进入图谱页或打开图谱弹窗

这条路径改动更小，也更贴近上游已有的信息架构。

### 7.7 扩展 manifest

宿主对插件暴露一个轻量 manifest 协议，例如：

```json
{
  "id": "colearn.knowledge-graph",
  "slot": "page",
  "container": "graph",
  "title": "知识图谱",
  "icon": "share-2",
  "data_endpoint": "/api/v1/colearn/graph/current",
  "visibility": {
    "modes": ["learning", "chat"]
  }
}
```

这里插件负责：

- 提供扩展声明
- 提供数据接口
- 提供当前 session / project 对应的数据查询参数

宿主负责：

- 读取 manifest
- 决定放进哪个固定槽位
- 用宿主内建容器完成渲染

如果沿用 `v0.2.1` 的 `apps` 入口形态，manifest 还可以补一类：

```json
{
  "id": "colearn.graph.entry",
  "slot": "apps",
  "kind": "utility_entry",
  "title": "CoLearn 图谱",
  "icon": "share-2",
  "target": "colearn.knowledge-graph"
}
```

这样宿主先只需要支持“Apps 入口挂件”，后面再演进到通用 `page` 槽位也不会推翻插件契约。

### 7.8 容器而不是任意前端 bundle

为了减少 NanoBot 升级时的前端断裂风险，不建议首版支持插件直接注入任意前端 bundle。

首版更适合支持少量内建容器，例如：

- `graph`
- `panel`
- `table`
- `timeline`

CoLearn 图谱页面只要声明 `container=graph`，再返回节点、边、聚类、当前激活节点等数据即可。

这样前端升级时，真正需要兼容的是宿主自己的容器组件，而不是每个插件各自携带一套前端运行时代码。

### 7.9 图谱展示的数据契约

图谱展示建议从插件侧返回统一结构：

```json
{
  "nodes": [
    {
      "id": "ml.model.basic",
      "label": "什么是模型",
      "kind": "concept",
      "state": "active"
    }
  ],
  "edges": [
    {
      "source": "ml.data.basic",
      "target": "ml.model.basic",
      "kind": "prerequisite"
    }
  ],
  "focus_node_id": "ml.model.basic",
  "legend": [
    { "kind": "concept", "label": "概念" },
    { "kind": "checkpoint", "label": "检查点" }
  ]
}
```

这份契约只表达图数据，不表达具体渲染实现。  
这样宿主可以在弹窗和页面之间复用同一份数据接口。

---

## 8. 数据所有权

### 8.1 唯一真相

新的数据真相划分：

| 数据 | 真相位置 |
|---|---|
| 课程知识 | `Wiki` 文件 |
| 学习状态 / 黑板 | `CoLearn Plugin State Store` |
| NanoBot session | 宿主缓存或镜像 |

### 8.2 为什么这样划分

如果把学习状态继续混在 NanoBot session metadata 里，会有三个问题：

1. 升级时宿主结构变化会反向污染 CoLearn
2. 教研很难审阅黑板真相
3. 未来换宿主时迁移成本大

所以插件化之后，学习状态由 CoLearn 自己存，宿主只保留必要镜像。

### 8.3 存储形态

MVP 继续采用易读、可 grep 的文件存储：

```text
.colearn/
  wiki/
  state/
    sessions/
      sid_xxxx/
        session.json
    projects/
  generated/
```

如后续并发和规模明显上升，再升级为 SQLite，不阻断当前方案。

---

## 9. 一次 turn 的执行流

### 9.1 主流程

```text
用户消息
  -> NanoBot 接收
  -> CoLearn 插件读取 session_id
  -> 判断 `SessionMode = CHAT | LEARNING`
  -> 读取黑板与学习状态
  -> turn start monitor 更新 `blackboard.runtime`
  -> 若为 `LEARNING`:
       -> 从 Wiki 找相关知识页
       -> 生成本轮学习上下文
       -> 注入 NanoBot turn
     若为 `CHAT`:
       -> 跳过学习主链，只保留轻量旁路
  -> NanoBot 执行回答 / 调 tool
  -> 插件接收 stream 和结果
  -> 若为 `LEARNING` 抽取学习事件
  -> turn end monitor 更新任务状态
  -> 更新黑板与状态
  -> 返回前端
```

### 9.2 Chat Mode 与 Learning Mode

`Chat Mode` 本质对应 `SessionMode = CHAT`：

- 主要由 NanoBot 直接完成普通对话
- CoLearn 插件只做轻量旁路
- 不主动展开完整学习链
- 不运行学习状态机
- 只保留必要 runtime 写回与命令入口

`Learning Mode` 本质对应 `SessionMode = LEARNING`：

- CoLearn 插件深度介入本轮 turn
- 加载 `blackboard.learning`
- 从 Wiki 取当前节点与先修关系
- 运行 `LEARN / CHECK / PAUSED`
- 写回学习事件与后续 continuation

### 9.3 模式切换与退出

首版建议提供最小切换语义：

- 进入学习模式：
  - UI 点击开始学习
  - `Apps` 进入 CoLearn
  - `/learn`
  - 明确学习目标与计划请求

- 退出学习模式：
  - `/chat`
  - UI 切回普通聊天
  - 用户明确表示“先不学了，随便聊聊”

学习 session 中的短暂跑题，不应立刻退出 `LEARNING`。  
例如一句玩笑、顺手问别的，不应该把整个学习链路打断。

### 9.4 知识注入方式

MVP 不做“整库大段拼接”，只做当前节点的局部注入：

- 当前概念页摘要
- 先修点
- 常见误区
- 当前黑板 blocker
- 本轮目标

这样更快，也更不容易把 prompt 撑爆。

---

## 10. MVP 范围与非目标

### 10.1 MVP 范围

MVP 包含：

- 本地 Wiki 课程知识底座
- 黑板与学习状态持久化
- `blackboard.learning / blackboard.runtime` 双分区
- `LEARN / CHECK / PAUSED`
- 与 NanoBot 的薄适配插件
- 基础查询工具
- `/colearn` 轻量黑板看板
- 前端继续消费现有黑板字段
- 宿主预留固定 UI 扩展槽位，支持图谱按钮与图谱弹窗或页面

### 10.2 非目标

本阶段明确不做：

- 自动知识图谱抽取为主链入口
- 全开放资料自动 ingest
- 多模态知识图谱
- 大规模在线协同编辑

---

## 11. 风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| NanoBot 内部 hook 或 loop 结构变化 | 中 | 所有宿主耦合收口在 `adapters/` |
| 当前 slim runtime 不支持自动发现外部 tool | 高 | MVP 采用显式注册；自动发现后置 |
| Wiki schema 设计过早定死 | 中 | frontmatter 保持小而稳，派生索引后置 |
| 状态机迁移引起行为漂移 | 高 | 先做 parity 测试，再迁移 |
| `blackboard.learning` 与 `blackboard.runtime` 混写 | 中 | 明确分区，运行态字段只写入 `runtime` 子域 |
| 前端字段与新黑板写回不一致 | 中 | 保持 `board_patch` 契约稳定，先兼容旧字段 |
| NanoBot 前端缺少稳定扩展接口 | 中 | 宿主只新增固定槽位与 manifest 协议，不开放任意 bundle 注入 |
| 本地文件增长后查询变慢 | 低 | 生成 `wiki_index.json` 与 link graph，必要时再上 SQLite 或向量索引 |

---

## 12. 对现有方案的修正

相对于上一版方案，本版做了这些关键修正：

1. **明确去掉 LightRAG 层，不进入首版设计**
2. **不再把外部 `nanobot.tools` 自动发现当成首版唯一路径**
3. **不再追求 CoLearn 全量逻辑塞进 NanoBot 内核**
4. **不再把 NanoBot session 当学习真相**
5. **继续保留 CoLearn 自己的状态机与黑板语义**
6. **前端图谱展示走固定 UI 扩展槽位，不假设 NanoBot 原生支持页面级插件注入**

---

## 13. 下一步

按本设计，后续先做四件事：

1. 定 `Wiki` 页面 schema
2. 定黑板 schema 与状态迁移规则
3. 定插件对 NanoBot 的最小宿主接口与 UI 扩展接口
4. 再进入实现阶段

→ 详细拆解见 [02-实施计划.md](./02-实施计划.md)
